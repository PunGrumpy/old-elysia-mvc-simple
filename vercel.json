{
  "github": {
    "silent": true
  },
  "buildCommand": "bun run build",
  "rewrites": [
    { "source": "/public/:match*", "destination": "/:match*" },
    { "source": "/(.*)", "destination": "/api" }
  ]
}
